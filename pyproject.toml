[tool.black]
target-version = ['py37']
line-length = 120
include = '\.pyi?$'
skip-string-normalization = true

[tool.isort]
py_version=37
line_length = 120

[tool.pylint.format]
max-line-length = 120

[tool.ruff]
line-length = 120
# pyflakes, pycodestyle, isort
select = ["F", "E", "W", "I001"]
